var constuctionPipeline = {
  run: function(){
    // Depends on resource information of the empire
    // We have a list of things that need to be constructed.
    // Then create a baked path and set available workers to it. [Priority?]
    //


  };
}
